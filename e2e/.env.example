E2E_BASE_URL=http://localhost:5173
API_BASE_URL=http://localhost:4100

# MongoDB URI is intentionally NOT duplicated here — globalSetup.ts loads it
# directly from ../server/.env (MONGODB_URI) so there is a single source of
# truth for the Atlas connection string.
