# API base URL. Leave empty in dev — the Vite proxy in vite.config.ts
# forwards /api/* to the local Express server. Set to the deployed API's
# origin only if the frontend and backend are served from different
# domains in production.
VITE_API_BASE_URL=
