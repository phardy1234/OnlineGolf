{
  "name": "online-golf-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:report": "vitest run --reporter=json --outputFile=test-report.json && node scripts/generate-test-report.mjs"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "mongoose": "^8.9.0",
    "selenium-webdriver": "^4.46.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/selenium-webdriver": "^4.35.6",
    "typescript": "~5.7.2",
    "vitest": "^4.1.10"
  }
}
